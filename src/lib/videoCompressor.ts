export const getFFmpegInstance = async (onProgress?: (progress: number) => void): Promise<any> => {
    return null;
};

export const compressAndTrimVideo = async (
    file: File,
    onProgress?: (progress: number) => void
): Promise<File> => {
    return file;
};

